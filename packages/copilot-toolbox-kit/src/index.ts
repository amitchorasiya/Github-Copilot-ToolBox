/**
 * Placeholder entry — replace with shared types, parsers, or CLI helpers
 * consumed by `github-copilot-toolbox` or other packages.
 */
export function toolboxKitVersion(): string {
  return "0.1.0";
}
