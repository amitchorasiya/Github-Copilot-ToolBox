{
  "name": "github-copilot-cursor-mcp-port",
  "private": true,
  "description": "Monorepo: GitHub Copilot Toolbox (VS Code) and Cursor→Copilot helpers. Build a VSIX with npm run package (runs packages/github-copilot-toolbox). Do not run npx vsce from the repo root — that manifest is not the extension.",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "compile": "npm run compile --prefix packages/github-copilot-toolbox",
    "test": "npm test --prefix packages/github-copilot-toolbox",
    "package": "npm run package --prefix packages/github-copilot-toolbox",
    "package:extension": "npm run package",
    "serve:site": "npx --yes serve docs -l 5173"
  }
}
